repository:
  name: thryftverse-upgrade
  description: ThryftVerse — flagship mobile marketplace
  private: false
  has_issues: true
  has_projects: true
  has_wiki: false
  default_branch: main

branches:
  - name: main
    protection:
      required_status_checks:
        strict: true
        # Required check contexts are the check-run names, i.e. each job's
        # `name:` field — not "Workflow / job-id". The previous entries
        # ("Frontend CI / typecheck", ...) matched nothing, so branch
        # protection would have waited on checks that can never report.
        contexts:
          - "TypeScript typecheck"
          - "Vitest unit tests"
          - "Design token + animated scroll lint"
          - "API build, unit, migration, integration"
          - "Key service build and tests"
          - "Mobile API contract typecheck"
      enforce_admins: true
      required_pull_request_reviews:
        dismiss_stale_reviews: true
        require_code_owner_reviews: true
        required_approving_review_count: 1
      restrictions: null
  - name: develop
    protection:
      required_status_checks:
        strict: true
        contexts:
          - "TypeScript typecheck"
          - "API build, unit, migration, integration"
      enforce_admins: false
      required_pull_request_reviews:
        dismiss_stale_reviews: true
        require_code_owner_reviews: false
        required_approving_review_count: 1
      restrictions: null

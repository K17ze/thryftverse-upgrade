# Editor & Upload Department — Gap Registry (2026-09-06)

Audit basis: two parallel read-only subagent audits (upload pipeline engineering + editor UI/UX),
plus current-date web research (Expo background upload guidance, expo-video deprecations).

## Status (updated 2026-09-14, Wave D — architecture + capture grammar)

| ID | Status | Evidence |
|---|---|---|
| EU-P0-1 fake progress | **CLOSED** | `ListingMediaStudio` renders `getItemProgress` → real queue byte progress (XHR transport). STATUS_PROGRESS deleted. |
| EU-P0-2 two crop systems | **CLOSED** | `InCanvasCropOverlay` deleted; `ListingMediaStudio` opens `CreatorCropSheet` (gesture crop + flipH/V + straighten + focal) for editable items. |
| EU-P1-1 legacy queue durability/connectivity | **CLOSED** | AsyncStorage snapshots + revive, NetInfo gate + `parkForOffline` (abort → pending, refunded attempt), AbortController cancel, +25% jittered backoff, multipart >10MB with per-part checkpoint. |
| EU-P1-2 focal point | **CLOSED** | Skia path + `FocalImage` honor focal everywhere focal can be authored; video surfaces can't carry focal (crop sheet is image-only by design — matches IG). |
| EU-P1-3 trim/thumbnail | **CLOSED** | Listing thumbs use `useVideoPoster` → `generateThumbnailsAsync` (real decoded frames). Per-clip trim is editor-domain; listing flow intentionally has no trim. |
| EU-P1-4 straighten/flip | **CLOSED** | `CreatorCropSheet` has straighten slider (a11y-labelled) + flipH/flipV; wired into listing cover edit. |
| EU-P1-5 undo | **CLOSED** | Poster doc-level undo/redo covers all `updateLayer` mutations (effects, crop results). `CreatorCropSheet` now has in-sheet undo: snapshot stack (crop frame, zoom/pan, rotation, flips, straighten, ratio) pushed at gesture/drag/press boundaries, undo button in top bar, cleared per session. |
| EU-P2-1..6 design defects | **CLOSED** | Stale — `ListingMediaStudio` rewritten: transparent 44pt glyph targets, 2-radius grammar, real poster frames, focal-aware `FocalImage`, hairline stroke grammar. |
| W12-P0-1 published video drops edits | **CLOSED** | Video renders through `renderComposition` at publish (fail-closed `MEDIA_RENDER_FAILED`); viewers consume `media_url` artifact via `pageWithRenderedMedia` (PosterViewerScreen + LookDetailScreen). |
| W12-P0-2 transcode drops authored ops | **CLOSED** | volume, fades, timed overlays (`enable=between`), reverse, freeze, speed curves all realized in the segment graph; fail-closed classification. |
| W12-P1-1 timeline/UI divergence | **CLOSED** | `usePosterTimeline` consumes `projectTimeline`; `handleSpeedCurveChange` writes `speed=averageSpeed(curve)`. |
| W12-P1-2 replace discards edits | **CLOSED** | A4 — replace preserves trim/speed/volume/effects, clamps trim, clears stale receipts. |
| W12-P1-3 UploadManager NetInfo | **CLOSED** | `setOnline` gate + offline abort→queued + reconnect resume; listing queue has same via parkForOffline. |
| W12-P1-4 segmented upload | PARTIAL | Multipart `resume()` pipelines parts (worker pool, concurrency 3). **Background upload session landed (2026-09-14):** single-PUT transport now routes through `nativeUploadTransport.putFile` → expo-file-system `File.createUploadTask` with `sessionType:'background'` (iOS URLSession survives suspension; Android always-background) for the creator queue + listing/profile flows; XHR remains the web/blob/multipart fallback with identical contract (real bytes, AbortSignal, ETag, 120s watchdog preserved). Transcode-upload overlap **landed server-side (2026-09-14)**: `streamOutput` render target → FFmpeg emits fragmented MP4 (`frag_keyframe+empty_moov`, append-only) to `pipe:1`; `runFfmpegStreaming` async-iterator pump (natural backpressure) feeds `StreamingMultipartUpload` → S3 parts upload at 8MB while encoding runs (`createMultipartUpload`+`uploadPartObject`+`complete`/`abort`; <8MB falls back to PutObject). Overlay retry opens a fresh session; failed attempts abort parts. Image/remux paths unchanged. Client→server overlap still needs a byte-range ingest protocol — parked. |
| W12-P1-5 mediaExportService dead | **CLOSED** | Adopted: "Export image" draft-export wired in Poster + Look composers (`exportDocumentImage` → MediaLibrary); video export stays gated behind the deferred native module. Viewer saves the baked `media_url`. |
| W12-P1-6 async publish processing | **CLOSED** | "Publish now" for render-heavy docs routes through `creator_schedules` immediate path (due-now → sweep worker → canonical transaction); doc `publishing` state, notification on completion, failure resets doc to `failed`. |
| W12-P2-1..3 polish | **CLOSED** | Safe-zone parity: shared `shared/safeZone.ts` contract (both composers had identical 52pt chrome but divergent guides — unified via `safeZoneInsets`). Capture grammar: slide-to-lock (measured pill hit-test, `recordLocked` survives release, tap-to-stop) + slide-to-zoom while holding (vertical drag → pinch-delta pipeline) — Snap/IG grammar complete. |
| ARCH-P1 creator root layout | **CLOSED** | 34 flat root files rehomed: canonical model/persistence → `core/projectStore/`, sheets → `surfaces/`, publish pipeline → `publish/`, shell/screens/context/canvas → `studio/`, camera → `capture/`, shared motion/analytics → `shared/`, view-document adapters → `export/`. 438 import specifiers rewritten programmatically; navigation layer stays thin (lazy `getComponent`, barrel imports). tsc + 33/33 tests green. |
| ARCH-P2 project fps grid | **CLOSED** | `canvas.fps` (CapCut `draft.fps` semantics — project output grid, default 30, optional for legacy docs). Trim/slip/split quantize to the frame grid; magnetic snap overrides the grid. Bonus fix: trim gestures emitted timeline-ms deltas consumed as source-ms (wrong at speed≠1) — snapping now resolves in timeline space then converts once via clip.speed; neighbor-boundary snap gated to same-source clips (split siblings). |
| PB-P0 poster video frames dropped edits | **CLOSED** | Multi-page posters rendered edited video pages (FFmpeg + HLS ran in `renderPosterFrameCompositions`) but the frame projection gated `renderedMediaUrl` on `isImage` — the video render was discarded and the frame silently published its source URL. Same defect class as W12-P0-1, surviving on the poster path. Fixed (2026-09-15): frames now consume `posterFrameRenders.renders.get(index)` unconditionally — trivial pages hold `null` and still fall back to `verifiedMedia.resolvedUrl`; rendered video frames store the m3u8/MP4 render URL. |
| PB-P1 rendered-video ABR parity | **CLOSED** | Published composition renders were flat progressive MP4s while uploaded media had an HLS ladder — viewers of edited video never got adaptive playback. Landed (2026-09-15): `uploadHlsOutput` extracted from `processVideoAsset`; `generateRenderedVideoHls(buffer, keyPrefix, durationMs)` in `pipeline.ts` probes the render (audio flag + duration), runs `buildHlsArgs`, uploads master/variant playlists + fMP4 segments under `renders/<doc>/<render>/hls/`; `renderCompositionMedia` swaps `renderedUrl` to `master.m3u8` when packaging succeeds (VideoManager already maps `.m3u8` → HLS playback; flat MP4 remains as graceful fallback + `<24h` cache). **Found while wiring:** `buildHlsArgs` was broken in production — it emitted `-vf:v:N scale=` per rendition *alongside* the `-filter_complex` chains, which FFmpeg hard-rejects ("simple and complex filtering cannot be used together"); `-map 0:a` was unconditional (fails on audio-less video); `closed-coder=1` is not a valid x264 option (replaced with `open-gop=0`). Meaning the HLS ladder had never successfully run for uploaded media either — progressive MP4 was serving everywhere. Fixed for both callers; `hasAudio` is now probed per-source. Real-FFmpeg smoke (`scripts/smoke-rendered-hls.ts`) verifies both audio and silent ladders: master + 5 variant playlists + fMP4 segments with `EXT-X-MAP`/`ENDLIST`. 102/102 tests green. |
| PB-P2 video artifact 3-URL contract | **CLOSED** | With `canonical_url` now an m3u8 playlist for every processed video, the single `media_url` field was doing three jobs — playback / poster-still / progressive download — and every image-context consumer broke silently. Landed (2026-09-15): migration 304 adds `poster_url`/`download_media_url` to `looks`, `posters`, `look_media`; `enrichPosterFrames` emits `posterUrl`/`downloadUrl` + `previewUrl` (COALESCE for highlight covers); `LOOK_SELECT_COLUMNS` + `look_media` carousel emit per-item `posterUrl`/`downloadUrl`; `VerifiedLookMedia` carries `posterUrl`/`progressiveUrl` (asset metadata poster + `public_url`); look create/PATCH/repost and both `look_media` insert paths write the columns; **repost now copies `look_media` rows** (carousel slides were silently dropped); legacy `POST /posters` joins `media_assets` for canonical/poster/download; feed.ts cover selects COALESCE to `poster_url`; `mediaAssets` serializer emits `posterUrl`/`downloadUrl`; creatorAnalytics thumbnails COALESCE'd. Frontend: `isVideoUrl`/`isVideoUri` match `.m3u8` (query-safe); `PosterViewerScreen` saves `downloadUrl`, shows `posterUrl` as `usePoster` still, prefetches next video's poster, refuses to save m3u8 without a progressive URL; all cover/tile/rail consumers (`LooksTab`, `ProfileLooksGrid`, `ProfileLookTile`, `LookMasonryTile`, `PosterStoryArtwork`, `PosterHighlightsRail`, `PosterArchiveScreen`, `CreatePosterHighlightScreen`, `HomeScreen` looks rail, `CreatorAssetPicker` look-sticker snapshots) prefer `posterUrl` for video; `discoveryFeedAssembly` no longer drops video looks/posters; `LookMediaCarousel` shows a poster still until `onFirstFrameRender`; `viewerAdapters` maps `posterUrl`→`thumbnailUri`. Verified: backend + frontend tsc clean; 116/116 touched tests. |
| PB-P3 listing-gallery poster parity | **CLOSED** | The 3-URL defect class extended beyond looks/posters: `listing_images.image_url` can hold the m3u8 `canonical_url` for video media, and every `images[]`/`imageUrl`/thumbnail projection fed it raw into image contexts. Landed (2026-09-15): `listingMediaImageUrl(item)` in `listingMediaProjection.ts` (poster still for `kind:'video'`); `listingImageUrls` routes through it — `media[]` keeps the playable m3u8+poster pair unchanged. All raw `mediaItems.map(m=>m.uri)` image arrays switched (index.ts ×5, feed.ts ×4, visualSearch.ts ×1, searchExtended.ts ×3) and `imageUrl` primaries resolve the poster for video-first listings. Attach path backfills `listing_images.poster_url` from `asset.metadata->>'posterUrl'` when the client can't supply it (processing finishes after upload). Raw-SQL thumbnail seams coalesced inline: 6 auction queries, `orderChatCards` item_image, `moodboards` picker subselects ×2, `streaming` session-card thumbnail, `visualSearch` feature-extraction source (decodes the URL — m3u8 fails as playlist text). `productDetailViewModel.isVideo` matches `.m3u8`. Verified: backend + frontend tsc clean; mediaContract 11/11 (+3 new); visualSearchRoute 12/12. `listings.image_url` itself is image-only by contract (cover gate requires `image/`). |
| PB-P4 chat media poster parity | **CLOSED** | Third surface of the 3-URL defect: chat video bubbles fed the canonical m3u8 into `CachedImage` and shared-media video tiles rendered empty dark blocks (no thumbnail was ever populated). Landed (2026-09-15): canonical media-asset lookup + ownership check hoisted *before* `chat_messages` INSERT (a failed check previously left an orphan message row); voice sends reject non-audio assets; new video messages merge `posterUri` (from `media_assets.metadata->>'posterUrl'`) into message metadata; `serializeChatMessageRows` and the shared-media endpoint backfill `posterUri` for legacy video rows via `chat_message_attachments → media_assets`. Frontend: `Message.posterUri` domain field; `chatApi` mapper + realtime `chat.message.created` payload extract it from metadata; append/hydrate paths carry it; `MessageBubble` renders `posterUri ?? mediaUri` for video bubbles (mediaUri stays for playback); `SharedConversationMediaScreen` maps `posterUri`→`thumbnailUri` on both API and store paths. Forward needs no change — resending the canonical URI re-derives the poster server-side. Verified: backend + frontend tsc clean; `chat.mediaPoster.test.ts` 6/6 (real `registerChatRoutes` over a fake pool: poster merge, no-insert on unknown/foreign asset, 422 non-audio voice, GET + shared-media backfill). |
| KF-P0 keyframe export | **CLOSED** | Landed server-side (2026-09-14): `keyframes` now survive `parseLayer` into `CompositionLayer` and force `transcode` (never trivial/remux — authored animation can't silently publish as source). Backend `keyframeEvaluator.ts` is a verbatim port of the frontend evaluator (same easings incl. spring, same first/last holds, same outgoing-keyframe easing). Rendered via per-frame sampled staircase expressions — the earlier "scale needs zoompan / opacity needs fade chains" concern was wrong: `scale=eval=frame` and `geq` alpha both accept per-frame `t`. Text: drawtext `x`/`alpha` exprs (position+opacity); text with scale/rotation kfs + all keyframed stickers route through a per-layer tight-PNG `-loop 1` input → `scale=eval=frame` → `rotate` → `geq` → centre-anchored `overlay`. Keyframed media layers animate then composite onto a canvas-colour `color` source. Bonus fix: static sticker rotation was silently dropped by the old composite path — now baked via sharp `rotate` (and folded into the animated chain when kfs exist). 18 evaluator + 66 renderer tests green (incl. classification, drawtext expr, animated-input, media-wrap, cheap-path regression). Residual: `modules/thryft-video-export` native on-device render remains the aspirational path (Edits/CapCut architecture). Static media transforms also landed: rotation/scale/position/size/opacity on the media layer now classify transcode and wrap onto the canvas (previously dropped). Real-FFmpeg smoke (`scripts/smoke-keyframe-render.ts`) verified end-to-end — extracted frames show text sliding+fading and the sticker growing+rotating; also caught two real bugs mocks couldn't: `-map '[0:a]'` passthrough label bug (pre-existing, silently retried) and `rotw(a)` misuse — rotate needs init-time `rotw(iw)` bounds and must precede the per-frame scale. |

## Reopened depth audit — 2026-09-15, upload recovery wave

Earlier CLOSED rows describe individual landings, not department-wide production sign-off. The fresh capture/upload audits found residual reliability failures. **100%/flagship convergence is not established.** Six other domain audits (timeline/keyframes, primitives, tools/sheets, studio/composers, publish/export, backend pipeline) failed at the worker rate limit and remain unaudited in this wave.

| ID | Priority | Status | Evidence / acceptance |
|---|---|---|---|
| UR-1 confirmation crash recovery | P1 | IMPLEMENTED, deterministic tests pass | `UploadJobStore.loadJobs`, `UploadManager.reconcileOnStartup` and `resumePendingJobs` recover confirming/stalled/initiating/uploading checkpoints; live attempts are not restarted. Receipt-only completed jobs are requeued from disk and cache. Native process-kill rehearsal pending. |
| UR-2 multipart replay receipt | P1 | IMPLEMENTED, deterministic tests pass | `uploads.ts` returns object/public URL, size/type, finalization and current media-asset state on replay, constrained by owner + bucket + key. Completed S3 sessions missing a DB receipt are verified and repaired without recompleting; `NoSuchUpload` is never accepted without object HEAD verification. `MultipartUploader.complete` refuses a receipt-only payload. Real DB/S3 failure-injection pending. |
| UR-3 session / signed-URL lifetime | P1 | IMPLEMENTED, deterministic tests pass | Seven-day session lifetime independent of unchanged short URL TTL. Legacy sessions get the same fixed deadline from initiation, not a rolling renewal. `/parts` returns the effective deadline; client consumes it rather than inventing +1h. True expiry still returns 410. Bucket incomplete-upload lifecycle policy must be verified against this window. |
| UR-4 non-video multipart policy | P1 | IMPLEMENTED, deterministic tests pass | `createMultipartUpload` accepts the declared size, validates against the matching media-type limit, and preserves the unknown-size server-generated streaming call signature. 15MB JPEG and AAC-container route cases pass; oversize helper rejects before S3. |
| UR-5 retry checkpoint ownership | P1 | IMPLEMENTED, deterministic tests pass | Multipart attempts reread the manager cache each time; a failed attempt retains completed part ETags and reuses one session. Store raw reads are separated from restart normalization so mutations do not reset other active jobs. |
| UR-6 permanent error classification | P1 | IMPLEMENTED, deterministic tests pass | API 4xx except 408/429 and typed terminal `MediaProcessingError` fail without job retry. Network/5xx/408/429 retry. Backend permanent HEAD mismatches return 422; transient HEAD errors remain retryable and never mark the session completed. Inner per-part retry classification remains a separate residual. |
| UR-7 single-PUT expired intent checkpoint | P2 | IMPLEMENTED, deterministic tests pass | Finalize 404/410 (`ApiRequestError.status`) now drops the persisted `uploadedObject` checkpoint so the next attempt re-presigns instead of replaying a dead intent forever. Covered by `it.each([404,410])` in `UploadRecovery.test.ts`, including the manual-retry re-presign path. |
| UR-8 multipart garbage collection | P2 | IMPLEMENTED, deterministic tests pass | `multipartSessionSweepHandler` claims expired `active` rows via a guarded status update *before* calling S3 abort — a concurrent complete sees a non-active row and gets 409 rather than being overwritten. `NoSuchUpload` marks the row expired cleanly; S3 failures leave the claim so the next sweep retries. Second pass aborts S3-side orphans: `listMultipartUploads` finds uploads with no session row past a 1h grace window (initiate creates the S3 upload before the DB insert — a crash between leaves an invisible leak). Aborts now pass the session's stored `bucket`, not just `config.s3Bucket`. Enqueued on a 5-minute bucket via `enqueueMultipartSessionSweepJob`; API + standalone-worker schedulers. Covered in `mediaPipeline.test.ts`. Bucket `AbortIncompleteMultipartUpload` lifecycle rule documented in DEPLOYMENT.md §6.5 as the ops backstop — still requires an ops-side apply. |
| UR-9 local URI memory pressure | P2 | IMPLEMENTED, deterministic tests pass | `MultipartUploader.resolveReadablePath` resolves `ph://`/`content://` to a ranged-readable `file://` path via `MediaLibrary.getAssetInfoAsync` (metadata only, no byte read), so library assets take the `position`/`length` chunked path. The whole-file Blob fallback now only serves non-library SAF document URIs — unreachable for the media-picker flows that produce ph://content://. |
| UR-10 background multipart parity | P2 | PARKED — deliberate, documented | expo-file-system `createUploadTask` uploads whole files only; ranged part PUTs would require staging each ~part-size chunk to a temp file (extra disk write per part + orphan-temp GC on crash). Per-part ETag checkpointing already makes a kill cheap — only the in-flight part retries on resume. Deferring temp-file staging until device profiling shows part loss is a real cost. |
| UR-11 cancellable project completion wait | P2 | IMPLEMENTED, deterministic tests pass | `waitForProjectCompletion(projectId, intervalMs, { signal, timeoutMs })` — abort and timeout both resolve with the latest job list (same status-inspection shape on every exit path) instead of looping forever on paused work. |
| UR-12 durable ingest enqueue | P1 | IMPLEMENTED, deterministic tests pass | `listClaimableProcessingJobs` + `mediaIngestReconcileHandler` re-drive `pending`/`retry`/stale-`processing` DB rows the queue never received. Re-enqueue uses a distinct jobId so a retained failed `media_ingest_${assetId}` BullMQ record can't suppress recovery; the DB claim stays the real arbiter so duplicate drives are no-ops. Wired through infra-queue dispatch, standalone workers, and an API scheduler (startup + interval + shutdown). Covered in `mediaPipeline.test.ts` (orphaned-job re-drive assertions). |
| UR-13 lifecycle races | P1 | IMPLEMENTED, deterministic tests pass | `resumeJob`/`retryJob` no-op on jobs with a live attempt (`activeUploads` guard) or a non-resumable status — a live job can no longer be flipped to `queued` mid-attempt and double-driven when the attempt frees its slot. `cancelJob` + store `updateJob` no-op on missing rows, so a racing in-flight `processJob` can't resurrect a cancelled job. Sweep claim-before-abort ordering closes the multipart complete/expiry race on the backend. Covered by `UploadRecovery.test.ts` + `UploadManagerReconcile.test.ts`. |
| UR-14 micro-state / efficiency | P2/P3 | IMPLEMENTED, deterministic tests pass | Done: stall→jobFailed, shared progress-persist throttle, `/parts` bounds, manifest validation, signal forwarding, dead-checkpoint clearance (UR-7). This wave closed the rest: `pendingEnqueues` map closes the async dedup gap between the existing-job check and `resolveFileSize`; `isInternetReachable === false` now parks the queue (captive-portal); terminal jobs purge on hydrate past a 7-day retention window; rolling 4s throughput window emits real `bytesPerSecond`/`etaSeconds` on progress events, aggregated per project via `getTransferStats`, surfaced in `SharingStateView`; all four multipart endpoints carry per-route rate limits (20/60/20/20 per min); `cleanupOrphanedUploadIntents` extracted from the orphan route and driven by a scheduled `orphan_upload_intent_sweep` infra job (the endpoint previously had no in-process caller). |
| CAP-1 capture safety | P1 | IMPLEMENTED, typecheck+lint clean; native device validation pending | Dismiss pan now gated on touch origin (top 120pt) + recording state via `onTouchesDown`/`manager.fail`, directional offsets (down-only activation, horizontal/upward fail) so it can't steal tray scrolls, sheet pans or shutter hold-drag. Auto-stop drives `recorderRef` directly (stale-closure fix). `pressHeldRef` aborts a recording start when the finger lifts during the mic-permission await. Both countdowns (photo timer, hands-free) are token-cancellable — toggle-off, second tap, unmount, close all cancel. Locked-recording release no longer leaves `isLongPressRef` stuck (swallowed-tap seam closed). `micState` derives from the native `status` — Android soft-denial is re-askable, only `denied`/`restricted` are blocked. Video captures render a real `expo-video` review + duration-tile tray thumbs instead of broken `<Image>` on mp4 URIs. Camera flip refuses mid-recording. Listing camera arms multi-capture via `initialMultiCapture`, enforces `maxCaptures` (was a dead `maxPhotos` prop), and no longer drops video captures. Residuals: 1241-line component needs domain extraction; GalleryCarousel error polish; native gesture validation pending (no ADB device). |

Validation evidence for this wave: frontend upload tests 29/29; backend media contract + composition/publication/keyframe tests 129/129; focused ESLint passes. New recovery cases were observed failing before production changes. Typechecks and review outcomes are recorded in the campaign progress ledger. Tests use real manager/store/route logic with fake native IO, storage SDK and DB boundaries; they are not native or real PostgreSQL/S3 evidence. ADB is available but `adb devices -l` returned no attached devices. Capture/UI geometry is unchanged in this wave.

Review disposition: inline-source independent frontend review approved the scoped delta with a medium residual: malformed successful completion payloads fail honestly but still consume bounded retries. Do not fabricate a 4xx status to classify that local contract error. API `fetchJson` was confirmed to throw `ApiRequestError` on non-OK HTTP responses; per-part signed URL expiry remains independently guarded when an older server omits session expiry. Backend review and two clean re-audit waves remain required before production closure.

Research, accessed 2026-09-15:
- **PRIMARY DOCUMENTATION:** AWS, *Uploading and copying objects using multipart upload in Amazon S3*, publication/update date not provided: https://docs.aws.amazon.com/AmazonS3/latest/userguide/mpuoverview.md — multipart sessions do not expire automatically; independently failed parts can be retried; incomplete parts incur storage charges until completed/aborted.
- **PRIMARY DOCUMENTATION:** AWS, *Download and upload objects with presigned URLs*, publication/update date not provided: https://docs.aws.amazon.com/AmazonS3/latest/userguide/using-presigned-url.html — URL validity is bounded separately, including credential lifetime. Seven days here is a product recovery window, not an extension of signed credentials.
- **PRIMARY DOCUMENTATION:** AWS, *CompleteMultipartUpload*, publication/update date not provided: https://docs.aws.amazon.com/AmazonS3/latest/API/API_CompleteMultipartUpload.html — completion can take minutes and an initial HTTP 200 can contain an error; AWS SDK handles parsing; NoSuchUpload may mean previously completed or aborted. Object verification is required before interpreting replay as success.

## Department map

- Listing upload flow: SellScreen.tsx / EditListingScreen.tsx -> ListingMediaStudio.tsx -> MediaUploadQueue (legacy) -> mediaUpload.ts (fetch PUT)
- Creator editor: CreatorCropSheet.tsx, CreatorAssetPicker.tsx, effects/adjustments/LUT, PosterComposerScreen timeline
- Durable upload: creator/core/upload/* (UploadManager, MultipartUploader, UploadJobStore) — creator flow only
- Profile: ProfileMediaEditor.tsx + useProfileMediaUpload.ts (direct uploadMedia, no queue)

## P0 — flagship blockers

### EU-P0-1 Fake upload progress on the main listing surface
- Evidence: ListingMediaStudio.tsx:50-57 STATUS_PROGRESS hardcoded (pending 0.1 / preparing 0.3 / uploading 0.65).
  mediaUpload.ts:149-155 uses fetch PUT — no onprogress exists in the legacy path.
- Impact: The single most-used commerce flow shows invented percentages. Users distrust the wait.
- Fix: Route listing uploads through XHR with upload.onprogress (pattern already proven in
  UploadManager.ts:743-805) and feed real bytes into the overlay.

### EU-P0-2 Two crop systems, one dead, neither reaches listings
- Evidence: CreatorCropSheet.tsx (destructive pixel crop, gesture-driven) vs InCanvasCropOverlay.tsx
  (non-destructive, zero importers = dead code). ListingMediaStudio wires only rotate/flip (lines 325-349,
  619-673); mediaTransforms.ts cropImage/cropToAspectRatio have no UI call sites.
- Impact: Users cannot crop listing photos at all — rotate/flip only. Inconsistent editor story across surfaces.
- Fix: Delete or adopt InCanvasCropOverlay; open CreatorCropSheet (crop+flip) from ListingMediaStudio cover Edit.

## P1 — materially below flagship

### EU-P1-1 Legacy queue is not durable / not offline-aware / weak cancel
- Evidence: mediaUploadQueue.ts in-memory only (:47-53); fails immediately offline (:414-427);
  cancelItem sets a flag but fetch is not aborted (:136-150, :372-395); no jitter in backoff (mediaUpload.ts:99).
- Fix: Unify on durable UploadManager (AsyncStorage jobs, AbortController, jittered backoff, multipart >10MB)
  for listings + profile; wire NetInfo pause/resume (UploadManager has no NetInfo listener today).

### EU-P1-2 Focal point ignored by all ExpoImage/Video preview paths
- Evidence: Set in CreatorCropSheet.tsx:332-342; honored only in Skia path (CreatorCanvas.tsx:1658-1669);
  ListingMediaStudio.tsx:530-537 and SortablePhotoStrip.tsx:212 use contentFit="cover" with no focal handling.
- Impact: Faces/subjects get cropped off-center despite user intent.

### EU-P1-3 Video trim trapped in poster composer; no video thumbnail generation
- Evidence: Trim only in PosterComposerScreen.tsx:1080-1204; CreatorAssetPicker only rejects >60s (:773-798);
  no expo-video usage for thumbnails (mediaTransforms.ts:261-275 is image-only).
- Note: expo-video-thumbnails is deprecated (SDK 56 removal) — use expo-video generateThumbnailsAsync.

### EU-P1-4 No straighten; flip missing from crop sheet; vertical flip unreachable
- Evidence: grep straighten = 0 hits; CreatorCropSheet has no flip; mediaTransforms.ts:125-136 supports
  vertical flip with no UI.

### EU-P1-5 Editor flows lack undo/recovery
- Evidence: CreatorCropSheet has only isProcessing + error toast (:303-329); no undo in crop/effects;
  PosterComposerScreen has keyboard undo only.

## P2 — design-language defects (the "bot-made" tells)

### EU-P2-1 Radius budget violated in one viewport
- ListingMediaStudio mixes Radius.xxl / .lg / .sm / .full in one screen (:742, :810, :838, :849, :860, :873);
  SortablePhotoStrip mixes .xl / .lg (:240, :254, :266). Charter budget: max 2 non-avatar radii per viewport.

### EU-P2-2 Mixed icon families in the same region
- Ionicons (CreatorAssetPicker.tsx:898, ListingMediaStudio.tsx:631, CreatorCropSheet.tsx:541) +
  AppGlyph coolicons (CreatorAssetPicker.tsx:843,1040) + AppIcon (LUTBrowserSheet.tsx:153) side by side.

### EU-P2-3 Stroke drift
- ListingMediaStudio.tsx:965 hardcodes borderWidth: 2 instead of Stroke.emphasis; family mixes 0.5/1/2 arbitrarily.

### EU-P2-4 Badge clutter on cover (label-everything disease)
- COVER + VIDEO + count badges + remove + edit stacked on one cover (ListingMediaStudio.tsx:803-842);
  every thumbnail carries number badge + cover pill + 15pt shadow (SortablePhotoStrip.tsx:221-245).

### EU-P2-5 Placeholder-grade empty state
- Dashed box + icon wrap + three lines of copy (ListingMediaStudio.tsx:736-752, :364-370) reads as scaffold,
  not authored surface. Verbose hint copy ("Well-lit photos from multiple angles sell faster").

### EU-P2-6 Raw numeric focal readout
- CreatorCropSheet.tsx:596 shows "Focal point: 58%, 42%" — machine output in a consumer UI.

## Research sources

- expo.dev/blog/faster-more-reliable-video-uploads-with-expo-modules — native background upload + S3 multipart
  is Expo's current recommended architecture (accessed 2026-09-06)
- docs.expo.dev/versions/latest/sdk/video-thumbnails/ — expo-video-thumbnails deprecated, use expo-video
  generateThumbnailsAsync (accessed 2026-09-06)
- rorklab.net multipart/TUS guide — iOS background execution limits, chunked resume patterns (secondary)

# Frontend Validation Audit — 2026-09-19 Item Re-check (audit0919)

Read-only re-validation of the frontend/product items from `thryftverse_fresh_production_audit_2026-09-19.md` against current code, including the post-audit campaign (discovery epoch guards, feed controls with undo, visual-search provenance, moodboard outcome states, replay fresh-URL retry, platform-pay gating, portfolio Paused, closet reorder modal, photo-gallery honesty, blocked-seller Q&A, refresh-preserves-cache, MAX_FONT_SCALE, notification contract).

Legend: **DONE** = verified in code; **PARTIAL** = implemented with a real gap; **MISSING** = not found; **EXTERNAL-ONLY** = code present but proof requires a physical device/backend run.

| Item | Status | Evidence | Notes |
|---|---|---|---|
| R24 — visual object-region search (crop→results) | **DONE** | `frontend/src/components/visualsearch/VisualSearchRegionCropper.tsx:114-449` (fullscreen cropper, PanResponder corners, MIN_REGION_FRACTION 0.04, whole-image→null at `:298-311`); wired via `VisualSearchQueryHeader` → `applyRegion` in `frontend/src/hooks/visualsearch/useVisualSearchResults.ts:200-204`; a11y increment/decrement at `:259-288` with `accessibilityRole="adjustable"` + region announcements `:248-255`; honest provenance — `queryScope` only set from server `retrievalMeta` (`:178`), note text says "within the framed area" only when backend confirms (`:254-266`); stale-response epoch+abort guards `:50-69`. | Full crop→search→results flow, contract-valid region math, screen-reader path. |
| R45 — live reconnect snapshot, gap-safe | **PARTIAL** | Manual `retry` re-runs the full snapshot path (`useLiveStreamSession.ts:262-270` → `connectToStream` `:61` + `fetchStreamChatHistory` `:112-114`); transport layer does auto-reconnect + per-topic seq gap replay via `/realtime/replay` (`frontend/src/platform/realtime/RealtimeClient.ts:439-470`); backend replay/seq endpoints `backend/api/src/routes/realtime.ts:270,305`. | Gap: `useLiveStreamSession` does **not** subscribe to `onResnapshot` (chat does — `useConversationMessages.ts:915`; CoOwn does — `useCoOwnOrderBookStream.ts:275`), so an unrecoverable gap (ring-buffer overflow) silently drops live events instead of triggering a snapshot refetch. `ConnectionState` also lacks a `reconnecting` state (`hooks/livestream/types.ts:8`) — a dropped socket inside a live session is invisible to the viewer UI. |
| R46 — live load test target connections | **MISSING / EXTERNAL-ONLY** | No k6/artillery/custom WS load harness found in `backend/api`, `scripts/`, or `frontend/` (searched `load test`, `artillery`, `k6`, concurrent-connection tooling). | Requires a device/load environment regardless; no code artifact exists. |
| R47 — live anti-snipe proof, deterministic | **DONE** | Constants `LIVE_LOT_ANTI_SNIPE_WINDOW_MS=30_000`, `EXTENSION_SECONDS=30`, `MAX_EXTENSIONS=5` at `backend/api/src/routes/liveLotEngine.ts:126-128`; deadline re-verify under lock at `:278-282`; sweep skips racing extensions `:612`; documented deterministic rule `backend/api/src/docs/AUTHORITATIVE_BOUNDARIES.md:84-86`; tests `backend/api/src/__tests__/liveLotTiming.test.ts:677-790` (extends inside window, no-extend outside, cap enforcement, reject at/after closes_at). Client merges `lot.extension`/`extensionCount` into the dock countdown `useLiveStreamSession.ts:149-211` and `useLiveBidActions.ts:80,113`. | Deterministic rule + unit tests + client propagation all verified. |
| R48 — live moderation (host/operator controls) | **DONE** | Host long-press moderation sheet mute/unmute/kick `frontend/src/components/livestream/LiveSellerLivePhase.tsx:78-144`, muted set seeded from `GET …/moderation/viewers` (`:83-90`); API `moderateLiveStreamViewer`/`listMutedLiveStreamViewers` `frontend/src/services/liveShoppingApi.ts:718-758`; viewer-side handling of `viewer_muted`/`viewer_kicked` `useLiveStreamSession.ts:227-241` (kick → terminal `removed` state; mute → composer disabled); viewer report `LiveChatList.tsx:15-71`; backend enforcement `backend/api/src/routes/streaming.ts:284-320,1099,1255,1428-1457` (mute blocks chat + new viewer tokens). | Host controls, viewer truth, server enforcement all present. |
| R57 — profile trust hierarchy, compact | **DONE** | `sellerTrust` (rating, completedSales, verificationTier, responseTimeLabel) surfaced compactly: `MyProfileScreen.tsx:277-278,402-404`; public profile `UserProfileScreen.tsx:283-284` → `UserProfileHeader`, `StorefrontAboutTab.tsx:160-161` response-time line, trader disclosure + passport modal (`UserProfileSheets` `:431-435`). | Compact trust strip + tiered verification, not a badge wall. |
| R58 — closet visual boards, shoppable | **DONE** | COLLECTIONS tab renders `collectionBoards` (`ClosetScreen.tsx:274-284`) → `CollectionDetailScreen.tsx` grids real listings (`:105-117`), tiles open PDP via `openProductDetail` (`:411`), `MoreLikeThisRow` shoppable rail `:426-505`. Boards are media mosaics (`ClosetMediaMosaic`, `ClosetCollectionsSection`), not grey placeholders. | Visual boards → tappable purchasable items. |
| R59 — settings reduce density, hierarchy | **PARTIAL** | Settings screen itself is reduced-density hierarchy (flat identity row + hairlines + health pills, `SettingsScreen.tsx:136-142`; `SettingsSection.tsx:62`); design-system density model `frontend/src/theme/density.ts:18-68`; per-surface user toggle only on Browse grid (`useBrowseGridDensity.ts`, AsyncStorage `thryftverse:browse-grid-density:v1`; `BrowseHeader.tsx:40-42`). | **Gap:** no global user-facing "reduce density" preference in Settings/`SettingsPreferencesContext` — density is author-controlled per shell, not a user setting. If the item meant a user control, it is MISSING; if it meant the Settings surface's own density, it is done. |
| R60 — notifications priority taxonomy (transactional vs social) | **DONE** | V2 structured contract `notificationViewModels.ts:103-162` (event-type→card map, never text-derived), attention levels critical/action/important/info (`:32-33`), `requiresAction` → "Needs attention" section ahead of time buckets (`groupNotifications:382-413`), social-only aggregation (`AGGREGATABLE_TYPES = like/price/new_item` `:273` — orders/resolutions never merged), server-side filter taxonomy `FILTER_EVENT_TYPES:441-490`, per-role rows (Commerce/Social/Financial/Auction/System components). | Transactional vs social separation is structural, not cosmetic. |
| R61 — creator export pipeline, native proof | **EXTERNAL-ONLY** (code present) | `frontend/src/creator/export/mediaExportService.ts:1-58` bridges `modules/thryft-media-export` (Skia/AVFoundation/Media3) with `jsExportImage` JS fallback; video via `modules/thryft-video-export` Nitro contract (`videoExportAdapter.ts`, `buildSingleClipVideoRequest`); intent presets for marketplace feed/HD. | Native render/save can only be proven on a custom dev client; native video implementation is documented as DEFERRED (`mediaExportService.ts:24-30`). Publish falls back to source media honestly. |
| R62 — interrupted upload resume | **DONE** | `frontend/src/services/mediaUploadQueue.ts` — AsyncStorage snapshots (`:78-93,188,510`), restore+resume on remount (`:408-411`), retryable classification (`:115-143`), per-item retry budget, `finalizationId` for finalize-only retry after transport drop (`:43-48`), abort-then-requeue-as-pending on interrupt. Consumers: `useProfileMediaUpload`, `useGroupMediaUpload`, listing/sell media pipeline. | Real durable resume, not just retry. |
| R63 — looks/product tagging, live inventory | **DONE** | `frontend/src/hooks/lookdetail/useLookTagHydration.ts:9-67` hydrates each tag against the shared listing-detail query cache; server row authoritative — sold state shown, null live price hides the price rather than showing a stale baked-in one (`:64-67`); `LookDetailScreen.tsx:126-135` re-resolves the inspect sheet tag live; `useLookInspect.ts:63` "This item is no longer available" on 404/410. | Tags never present stale price/availability as current. |
| R64 — moodboard undo/redo durable | **DONE** | `frontend/src/components/moodboard/useMoodboardHistory.ts` — inverse ops recorded only when the forward op is `applied`/`queued` (durable outbox) (`:106-184`), conflict clears both stacks (`:277`); durable outbox `frontend/src/storage/moodboardOutbox.ts`; UI undo/redo buttons with a11y labels `MoodboardEditorScreen.tsx:221-251`; version-history + conflict sheets wired `:522-546`; tests `src/__tests__/moodboardHistory.test.ts`, `moodboardHistoryOutcome.test.tsx`. | Undo survives offline via outbox; failed ops never land on the undo stack. |
| R65 — seller task-first hub, action priority | **DONE** | `SellerHubScreen.tsx:233-241` — `splitTasks`, `topTask`, `summarizeTriage` (pendingOrdersCount, atStakeGbp), `tasksStale` flag across orders/offers/payout_holds/catalog_imports/verification_demands; task routing `components/seller/useSellerHubTaskNavigation.ts`; pillar tiles + task queue ordering `:271-289`; sticky primary dock `:384`. | Action-priority triage, not a metric dashboard. |
| R66 — seller bulk edits, audited | **DONE** | `frontend/src/hooks/inventory/useInventoryActions.ts` — bulk pause/resume `:141-224`, bulk delete `:226-299`, bulk edit `:319-414`; per-item receipts applied truthfully (applied/rejected/unknown, only failed rows restored `:164-190`); deterministic idempotency keys `bulk-{cmd}-{sortedIds}` / `bulk-edit-{hash}-{nonce}` (`:158,247,350`); optimistic patch + snapshot-restore + reconcile refetch. UI: `InventoryBulkActionsBar`, `BulkEditSheet` (`InventoryManagementScreen.tsx:272-320`). | Batch commands go through `submitSellerHubBatchCommand` with per-item receipts — audited per item. |
| R67 — offer to likers, controls | **DONE** | `frontend/src/components/product/OfferToLikersSheet.tsx` — discount presets 10/15/20/25 (`:41`), custom price (`:102-135`), expiry 24/48/72h (`:42`), liker count from real listing likes (`:118`); backend fan-out `sendOfferToLikersOnApi` → `POST /listings/:id/offers-to-likers` (`services/listingOffersApi.ts:227-261`). | Real controls + real endpoint. |
| R68 — net proceeds, fee-aware display | **DONE** | Sell flow after-fee hint `SellScreen.tsx:695-712` (`calculatePlatformChargeGbp`, same fee structure as checkout); auction start-bid fee hint `components/sell/AuctionFieldsSection.tsx:79-80`; Smart Sell min-net-payout model with fee breakdown `components/sell/SmartSellCard.tsx:251-394` (`computeNetQuote`, platform-fee line `:390`); pending proceeds list `SellerEarningsScreen.tsx:206-222` incl. dispute hold state. | Fee-aware net display at authoring + earnings surfaces. |
| R69 — analytics metric dictionary, canonical | **DONE** | `backend/api/docs/METRIC_DICTIONARY.md` — canonical cross-domain dictionary (471-line scope rule, every row cites an emission site; Prometheus `src/lib/metrics.ts`, PostHog `frontend/src/analytics/track.ts`, creator analytics `src/domain/creatorAnalyticsContracts.ts` `METRIC_VERSION='creator-analytics-2'`). | Doc-based dictionary with code-cited definitions, dead definitions explicitly segregated. |
| R70 — ledger-backed net metrics, reconciled | **DONE** | Contract `frontend/src/services/commerceApi.ts:1075-1082` — `netSalesGbpMinor = revenue − refunds − fees`, null when ledger absent, `completeness:'complete'|'partial'` flag; UI honesty `components/seller/analytics/useAnalyticsInsights.ts:23-35,61-67` — labels "Net sales" only when complete, else "Revenue"; stale-period labelling `useSellerAnalytics.ts:99-151,229-230`; backend ledger gating `index.ts:2653-2658` (`ledgerTablesAvailable`) + ledger_entries queries `:4719-4768`; reconciliation lib `backend/api/src/lib/reconciliation.ts`. | Net metrics are ledger-derived and honestly labelled when partial. |
| R71 — VoiceOver/TalkBack matrix | **DONE** (code) / device matrix EXTERNAL | 3038 `accessibilityLabel` usages across 948/1306 `.tsx` files; `eslint-plugin-react-native-a11y` static enforcement (`useA11yAudit.ts:18-20`); runtime dev audit `utils/accessibilityAudit` + `useA11yAudit`; adjustable-role cropper actions (R24); a11y hints/roles pervasive on actionable controls. | Actual VoiceOver/TalkBack traversal passes per screen still need device runs. |
| R72 — large text, no clipping | **DONE** | Tiered `MAX_FONT_SCALE` `frontend/src/theme/typography.v2.ts:289-295` (`utility`/`heading` tiers); `maxFontSizeMultiplier` applied pervasively (466+ hits — checkout footer, bid panels, chat banners, reorder modal `ClosetGrid.tsx:86`, etc.); `numberOfLines` + flexible layouts on value rows. | Migration evident; residual per-screen clipping can only be ruled out with device sweeps. |
| R73 — mid-range Android perf budgets | **EXTERNAL-ONLY** (instrumentation present) | Native perf module `frontend/src/platform/performance/` (`ThryftPerformance` MetricKit report, `useSurfacePerf`, no-op stub when unlinked); visit-scoped readiness milestones `performance/visuallyComplete.ts` (mounted/data-ready/first-media/interaction-ready/visually-complete, derived never asserted); dev render trace `renderTrace.ts`; video QoE telemetry `video/VideoManager.ts:148-311` + `qoeSchema.ts` thresholds. | Budgets enforced on-device only; nothing verifies FPS/memory here. |
| R74 — adaptive video + failure handling | **DONE** (code) / playback EXTERNAL | `video/VideoManager.ts` — pooled players, decoder budget by device tier (`:40-49`), HLS/`contentType:'hls'` ABR, QoE TTFF/rebuffer/bitrate telemetry, error handling `:184`; replay failure → fresh-URL refetch `LiveStreamReplayScreen.tsx:252-269` (retry replaces stale signed URL via epoch-guarded reload); honest playback-failed state `:189`. | |
| R75 — media moderation quarantine, client-visible | **DONE** | Upload pipeline state `services/mediaUpload.ts:93,112,312-366` — `quarantined` + `quarantineReason` surfaced to the uploader; voice-message ladder `pending/allowed/limited/blocked` rendered `components/chat/VoiceMessageBubble.tsx:25-159` (blocked unplayable, `useVoicePlayer.ts:176`); catalog-import quarantine state `services/catalogImportApi.ts:77`. | Client-visible quarantine states exist on both producer and consumer sides. |
| R76 — offline unknown-outcome, no fake success | **DONE** | `frontend/src/hooks/useUnknownOutcomeReconciliation.ts` — unknown_outcome → poll lookup-by-idempotency-key → acknowledged/safe_to_retry/unresolved with exponential backoff (`:90-163`); consumed by money mutations, e.g. `MakeOfferSheet.tsx:339-380` (network error + idempotency key → reconcile instead of claiming failure/success); batch unknown items reconcile via refetch `useInventoryActions.ts:187-189`. | |
| R77 — realtime event versions + gap recovery | **DONE** | Envelope `{id,topic,type,payload,timestamp,seq?,v?}` — backend `lib/realtime.ts:18-19,388-402` attaches seq+version; client `RealtimeClient.ts` tracks `lastSeenSeq` per topic (`:103,370-375`), on reconnect checks `/realtime/seq` + replays `/realtime/replay` (`:439-470`), emits `resnapshot` when gap exceeds buffer (`:234-241`); backend routes `routes/realtime.ts:270,305,329-340` (`canReplayGap`). Consumers: chat `useConversationMessages.ts:915`, CoOwn order book `useCoOwnOrderBookStream.ts:275`. | See R45 note — live session topic gets transport replay but lacks a resnapshot listener. |
| R78 — API idempotency catalog | **DONE** | `backend/api/src/docs/MUTATION_IDEMPOTENCY.md` — generated catalog, 471 mutation routes classified by mechanism (idempotency-key/state-machine/on-conflict/locked-update/unguarded), money-path unguarded list; generator `scripts/mutation-idempotency-catalog.mjs`. | 218 routes flagged unguarded — catalog exists; residual risk is tracked, not zero. |
| Section 8 — feed two-stage ranking + diversity + negative intent | **DONE** | Backend `routes/recommendations.ts`: multi-source candidate pool merged to cap 800 (`:31,1112`), intent-ledger hard exclusions + `not_interested`/`report_content` item suppression (`:849-919`), directive matching (`:222-242`), decision-service scoring with exploit/explore policy, then `rerankCandidates` (`:1448-1522`) — component scores (freshness/purchase/save/view/sellerTrust/badOutcomeSuppression) + seller-diversity cap (max 3 per seller in top 5, `:1499-1517`). Client `useForYouFeed.ts` carries serveMode/policyVersion/componentScores; feedback `recommendationFeedbackApi.ts` (not_interested/show_fewer writes + `undoItemNotInterested` `:193`); UI `DiscoverScene.tsx:259-344` + undo toasts `UnifiedDiscoveryScreen.tsx:311,362`; "why am I seeing this" explanation sheet. | Retrieval → rank → diversity rerank is real; negative intent is durable server-side. |
| Section 71 — screen state checklists (spot-check) | **DONE** (spot-checked 6) | Home `HomeScreen.tsx:374-543` (skeleton only on empty first load, honest For-You empty/error, no silent substitution, offline flag); PDP `ItemDetailScreen.tsx:209-253,424-440` (loading/error/offline canvases, partial-error banner, offline last-good); Checkout `CheckoutScreen.tsx:511-951` (skeleton, EmptyState, offline banner, per-section inline errors + retries, a11y focus on payment error `:422-424`); Chat `ChatScreen.tsx:282-841` (offline, sync retry, upload/send retries); Inbox `InboxScreen.tsx:184-215` (offline banner, sync banner, empty-state retry); TradeConfirm `TradeConfirmScreen.tsx:440-483,503-632` (network-error→not-placed honesty, quote retry). | All spot-checked screens cover loading/empty/error/offline. Full 16-screen sweep not exhaustively re-opened, but the pattern is consistent across the flagship set. |

## Campaign-item spot verification (all confirmed in code)

- **Discovery epoch guards + per-module stale retry:** `useDiscoveryContent.ts:39-98` (monotonic epoch, per-module `staleModules`, fulfilled/rejected split); `useDiscoveryFeed.ts:99` stale retry banner.
- **Visual-search provenance honesty:** `useVisualSearchResults.ts:139-187` — provenance travels with the collection; cache substitution ⇒ `filter_only` + 'partial' banner, never claims a visual/region match that didn't run.
- **Moodboard board-op outcomes:** applied/queued/conflict/failed in `useMoodboardHistory.ts:106-184` + `useMoodboardMutations.ts`; conflict-compare + version-history sheets.
- **Replay fresh-URL retry:** `LiveStreamReplayScreen.tsx:256-269`.
- **Platform-pay device gating:** `CheckoutScreen.tsx:448-452` (`isPlatformPaySupported` probe), gated render `:650,976-977` per-OS + `isPaymentMethodAllowed`.
- **Portfolio Paused status:** `PortfolioScreen.tsx:214` — paused position renders "Paused", not collapsed to Closed.
- **Closet/profile reorder FlashList modal:** `components/myprofile/ClosetGrid.tsx:41-105` — reorder runs in a modal FlashList (never mounts 1000 media views inline); save order via `useMyProfileReorder.ts`.
- **Profile listings navigation:** `openProductDetail` wired in `UserProfileScreen.tsx:200,215,307`, `MyProfileScreen`, `ClosetScreen.tsx:257,269`.
- **Photo-gallery honesty:** `CommerceMediaStage.tsx:89-251` — "Photo unavailable" + retry remount per slide; `CachedImage.tsx:388-465` failure fallback.
- **Blocked-seller Q&A:** `ItemDetailSheets.tsx:51-54,307` forwards `isSellerBlocked` to `ListingQA`; seller section suppresses follow/message when blocked (`ItemDetailSellerSection.tsx:29-70`).
- **Refresh-preserves-cache:** `useForYouFeed.ts:217-225` (refresh failure keeps last-good page + inline `refreshError`); `BackendDataContext.tsx:113-125` (failed refresh falls back to last-good, source flips to 'cache').
- **MAX_FONT_SCALE migration:** `typography.v2.ts:289` + site-wide adoption.
- **Notification contract alignment:** `notificationsApi.ts:133+` V2 registry + `upgradeToV2` (`:911`); view models consume structured fields only.

## Residual gaps worth tracking

1. **R45**: live session hook has no `onResnapshot` subscription and no `reconnecting` connection state — worst-case gap silently drops live events.
2. **R59**: no user-facing global density preference (if required).
3. **R46**: no live-connection load-test harness exists.
4. **R61/R73**: native export and perf budgets are code-complete but device-verifiable only.
5. **R78**: catalog exists; 218 routes still flagged `unguarded` (heuristic — needs review, not necessarily fix).

fix(a11y): add reduced-motion support to 22 screens with entrance animations

All screens using FadeInDown/FadeInUp/SlideInDown/SlideInUp entrance
animations now check useReducedMotion() and pass undefined as the
entering prop when reduced motion is active. This ensures the app
respects the user's OS-level reduced motion preference per AGENTS.md
section 17 (Motion and Interaction).

Screens updated: BalanceHistory, CategoryDetail, CategoryTree, Closet,
CoOwnIssue, CollectionDetail, CreateCollection, EditCollection,
ExploreCollection, GlobalSearch, Home, ItemDetail, OrderSupport,
OutfitBuilder, PulseFeed, Search, SupportTicketDetail, TradeConfirm,
TwoFactorSetup, VisualSearch, Withdraw, WriteReview.

Also updated visual14 test assertions to match the wrapped entering
prop pattern (checking for the FadeInDown chain without requiring the
exact entering={ prefix).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>

fix(theme): migrate AccountControlScreen to useAppTheme

Per audit section 5.1: "102/120 screens still import the legacy
constants/colors module" and "StyleSheet.create() values using the
mutable legacy Colors export are calculated at module load."

Migrated AccountControlScreen from legacy Colors to useAppTheme().colors:
- Removed `import { Colors } from '../constants/colors'`
- Added `import { useAppTheme } from '../theme/ThemeContext'`
- All 54 `Colors.xxx` references replaced with `colors.xxx` from
  useAppTheme() hook
- ConsequenceRow sub-component given its own useAppTheme() call
- Static StyleSheet stripped of color properties; colors applied inline
  for theme reactivity
- The screen now supports live theme switching without remounting

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>

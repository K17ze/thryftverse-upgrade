fix(a11y): touch targets, reduced-motion on decorative pulses & skeletons

Per audit section 5.4: "Visual Search runs a JavaScript setInterval
every 16ms to animate a fake scan line, with no reduced-motion
alternative" and "Confirmed ProductCard save/favourite controls are
visually 32x32pt without an explicit 44/48pt hit region."

Touch target fixes:
- Bumped hitSlop from 4 to 8 on 5 AuctionDetailScreen floating
  controls (back, share, save, like, watch) for 50pt+ effective area.
- Bumped hitSlop from 6 to 8 on ProductCardV2 save button,
  VisualSearchScreen close button, and 4 AuctionMarketHeader actions.
- Bumped LookSocialActions button paddingVertical from 10 to 12 for
  46pt minimum height (was 42pt — below 44pt minimum).

Reduced-motion fixes on continuous animations:
- ActivityBadge PulsingDot: static opacity when reduced motion enabled.
- PulseDot (chat presence indicator): no pulse animation when reduced
  motion enabled.
- AvatarRing (unread glow): no pulse animation when reduced motion
  enabled; ring still shows as solid border.
- PremiumSkeletonTile shimmer: static position when reduced motion.
- SkeletonChatLoader breathing: static opacity when reduced motion.
- RefreshIndicator rotation: no spin when reduced motion; indicator
  still shows but without continuous rotation.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>

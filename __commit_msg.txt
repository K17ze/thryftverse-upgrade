feat(P1): Agent Platform truth, Chat decomposition, anti-prototype cleanup

Agent Platform (specs 03/04/05, acceptance matrix):
- Dynamic model discovery: probeProviderConnection now parses
  provider /models response, returns DiscoveredModel[] with
  capabilities. discoverModels() with 6h TTL cache. Hardcoded
  model catalogue replaced with provider-authoritative list.
- Pause all agents: pauseAllAgents() clears in-memory deployment
  registry, records to activity ledger. Control added to
  AIAgentIntegrationScreen with truthful session count.
- Activity ledger: agentActivityLedger.ts (append-only, max 500
  entries) records deploy/remove/pause/tool/approval actions.
  AgentActivityScreen displays ledger with loading/empty/populated
  states. Route registered in navigator + command palette.
- Real connection probe verified intact from P0 (format validation
  alone never produces "Connected").

Chat V3 controller decomposition (spec 16):
- ChatScreen wired to useConversationMessages + useConversationComposer
  hooks. Removed ~985 lines of inline state, effects, handlers.
  ChatScreen reduced from ~2309 to ~1324 lines.
- useConversationMessages owns: message list, API sync, sending,
  retry, delete, offer auto-send, date separators, scroll helpers,
  NetInfo/AppState listeners, markConversationRead.
- useConversationComposer owns: text input, reply-to, attachment
  picker, voice recording, reacting-to, search, cross-device
  composer state hydration + persistence.
- 5 hooks remain inline (commerce, agents, safety, selection) —
  safe incremental wiring, no breakage.
- Hook interfaces adapted to match ChatScreen's working behavior
  (SuggestedReply type, SupportedCurrencyCode, formatFromFiat).

Anti-prototype cleanup (acceptance matrix):
- Math.random persisted IDs replaced with makeStableId (uses
  crypto.getRandomValues) across 30+ locations in 20+ files.
  Remaining Math.random usages are UI animations, demo-mode data
  (gated behind __DEV__), file names, and transient UI state.
- 13 demo mode flags gated behind __DEV__ (were `true` in
  production): aiPhotoEnhancement, searchAutocomplete,
  listingQuality, galleria, inAppNotifications, smartSell,
  conversationalSearch, algorithmTransparency, moodboard,
  AIPreferences, DataPrivacy, NotificationPreferences,
  SustainabilityPreferences.
- 12 files: mockData type-only imports converted to `import type`.
- mockData runtime imports verified already gated behind
  ENABLE_RUNTIME_MOCKS (false in production).
- No fake editorial data found in production screens.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>

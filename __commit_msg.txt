feat(creator): Poster Composer V3 split + navigation wiring

Per spec 09 (Poster Architecture V3), Poster gets its own dedicated
frame-native composer screen separate from the generic Studio shell.

- Created frontend/src/creator/poster/PosterComposerScreen.tsx as a
  dedicated frame-native composer for Poster. Wraps in CreatorProvider
  with initialType="poster" and renders CreatorStudioInner.
- Exported CreatorStudioInner from CreatorStudioShell so
  PosterComposerScreen can wrap it.
- Updated CreatorStudioScreen branch: type='look' → LookComposerScreen,
  type='poster' → PosterComposerScreen. Both are now dedicated
  composers, not a shared shell with isLook/isPoster branching.
- Added PosterComposerScreen to creator/index.ts exports.

The frame-native UX refinement (simplified chrome, filmstrip overview,
More/Advanced for Layers/Opacity/Safe zone) is an ongoing pass per
spec 09 acceptance journey. The structural separation is complete.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>

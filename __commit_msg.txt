chore: remove anti-AI production residue comments + add CI gate script

Removed 55 design-by-reference comments across 18 files that created an
"AI-generated" impression. Only comments were modified — zero code logic
changes.

Patterns cleaned:
- "Instagram pattern" (26), "Snapchat pattern" (16), "TikTok pattern" (2)
- "flagship 2026" (4), "premium glassmorphism" (1), "Psychology" (9)

Each comment had the pattern label removed while preserving the useful
technical explanation that remained.

New CI gate: scripts/check-production-residue.mjs (471 lines)
- Scans 814 source files for ERROR patterns (DEMO_MODE=true, Math.random
  IDs, AsyncStorage secrets, ungated mockData imports, fake- prefixed
  declarations) — exit 1 on violation
- Reports WARNING patterns (competitor references, design-by-reference
  labels) — exit 0, non-blocking
- Added as "check:residue" npm script

Current scan: 0 errors, 35 warnings (all non-blocking per-spec/per-audit
references). CI gate passes.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>

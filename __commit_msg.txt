polish(P1): flagship visual grammar for Co-Own, Wallet, Chat composer

Co-Own Asset Detail V3 (spec 14):
- Title promoted to screenTitle (24pt bold) as dominant text object;
  price remains priceHero (28pt) as dominant numeric anchor.
- Icon grammar: metadata glyphs 12pt → 14pt (checkmark-circle,
  chevron-forward) to comply with 14-18pt metadata band.
- Deliberate spacing: replaced uniform gap with relationship rhythm
  (title group tight at 4pt, issuer/price separated at 16pt,
  availability tight to price at 4pt).
- Viewport 2 spacing: eliminated 48pt double-lg gap, added
  holderPosition top padding for consistent separation.
- Skeleton geometry aligned to collectible-first layout (removed
  detailTransaction card surface, restructured to match flat layout).

Wallet V3 (spec 17):
- Privacy eye toggle added (was missing) — eye/eye-off icon in hero
  header, balance shows •••••• when hidden.
- Add money promoted to full-width dominant primary button (was
  sharing equal weight with Redeem, muddying hierarchy).
- Withdraw moved to restrained secondary row alongside Redeem.
- Safeguarding consolidated into single disclosure card (was 2
  competing cards + duplicate in breakdown).
- Decorative subtitle "Your 1ZE settlement balance" removed from all
  CoOwnMarketHeader instances.
- Icon grammar: Redeem icon changed to swap-horizontal-outline (was
  colliding with Withdraw's arrow-down-circle-outline).
- Removed unused quickActionsRow, QuickAction component, dead styles.

Chat V3 composer (spec 16):
- Agent chip consolidation: replaced per-agent chip strip with single
  quiet indicator ("2 agents" when 2+ deployed, agent name when 1).
- Suggestion stacking prevention: agent suggestions and quick replies
  are now mutually exclusive (only one suggestion area at a time).
- Long press message → Ask agent about this: ADDED (was missing).
  Wired to open ChatAgentPicker or pre-fill composer with @agent.
- Composer icon grammar: add-outline 26pt → 24pt (composer action
  band). Send button 40pt → 44pt (full touch target).
- Removed decorative SuggestedRepliesBar header (agent icon circle +
  "X suggests" label + "demo" badge) — decorative chrome violation.
- Removed double-hairline on banners (bannerDivider was creating
  visible double-hairline gap).
- Removed 4 dead styles from removed permanent agent chip strip.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>

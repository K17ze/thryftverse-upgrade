import { VisualSearchClient } from '@/components/visualsearch/VisualSearchClient';

export const metadata = {
  title: 'Search by photo',
};

export default function VisualSearchPage() {
  return <VisualSearchClient />;
}

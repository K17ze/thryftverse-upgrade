Wave 6: Polish & motion — token discipline, haptics, geometry-stable transitions

Motion token discipline (subagent 9141d555):
- Replaced hardcoded durations with Motion.duration.* tokens across 28 files
  (touch/fast/normal/slow/slower/crawl semantic scale)
- Replaced hardcoded spring configs with Motion.spring.* presets across 9 files
  (tap/gentle/settle/bounce/press/lift/glide/sharedElement/indicator/success)
- Duration scale already reconciled (designTokens re-exports Motion.duration)
- Easing.linear kept only where physically correct (typewriter, progress ring, spinner)

Haptic discipline (subagent 54712ee1):
- Eliminated all direct expo-haptics imports in screens/components/hooks (10 files)
- Only canonical helpers (utils/haptics.ts, hooks/useHaptic.ts) import expo-haptics
- Semantic mapping enforced: light=selection, medium=commitment, success=completed,
  warning=retry failure, error=validation/upload failure, heavy=destructive
- Double-tap like now uses compound like pattern (two quick taps, not single Medium)
- Fixed retry-failure defect: was Medium impact, now warning semantic

Geometry-stable transitions (subagent 91b99949):
- StateTransition crossfade duration → Motion.transitions.mediaLoad (250ms opacity-only)
- ItemDetailScreen + AuctionDetailScreen: content fades in via mediaLoad crossfade
  (no pop when skeleton unmounts)
- Reduced-motion compliance: AnimatedBadge, AnimatedCounter, Confetti, RefreshIndicator,
  SkeletonLoader all respect useReducedMotion (collapse travel, keep opacity)
- SkeletonLoader systemic fix: all 15+ skeleton consumers now get reduced-motion
  compliance automatically via internal useReducedMotion() call

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>

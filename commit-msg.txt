Wave 3: Growth surfaces + forbidden token codemod

Wave 3 — Growth surfaces:
- Profile hierarchy: MyProfileIdentityHero rewritten with seam-row composition
  (avatar overlaps cover, stats right-aligned), followers as hero stat,
  single compact trust line, bio constrained to 2 lines, single-row actions
  with primary/secondary hierarchy. UserProfileScreen: removed card-on-card
  trust stacking, removed dead Collections/Drops tabs and fabricated idleQuery.
  ProfileShopTile: restrained empty-image placeholder. ClosetScreen: added
  collections loading skeleton matching board card geometry.
- Creator studio capture-first: PosterComposerScreen removed redundant context
  toolbar, added actionable empty-canvas CTA, IconGrammar token compliance.
  LookComposerScreen flattened AI Effects card, added capture-first CTA,
  IconGrammar compliance. CutoutPreviewSheet added retry button on error state.
  GreenScreenSheet fixed raw transition literal, IconGrammar compliance.
- Sell flow truthful AI: AIBadgeField now hides "Suggested" badge when user
  edits a field (becomes user-authored). Analyzing overlay honestly describes
  heuristic ("Reading photo metadata"). Price no longer auto-filled — seller
  picks from suggested range. SmartSellCard removed fabricated statistics.
  ListingQualityMeter rounds scores to nearest 5 (honest precision).

Flagship primitive token fixes:
- FlagshipHeader: bodyEmphasis to bodyStrong, PressScale.gentle, off-grid spacing
- FlagshipNavigationRow: bodyEmphasis to bodyStrong, captionElevated to caption,
  IconGrammar.metadata, PressScale.gentle
- FlagshipMetricLine: bodyEmphasis to bodyStrong
- FlagshipFormSection: metaElevated to label
- Added TypeStyles.bodyStrong canonical alias to designTokens.ts

Forbidden token codemod:
- 1795 replacements across 352 files (captionElevated, metaElevated, bodyLarge,
  bodyEmphasis, priceLarge, price → canonical names)
- Remaining ~90 in 40 files being cleaned up by follow-up subagent

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>

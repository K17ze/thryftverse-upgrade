Wave 1-2: Foundation + money surfaces flagship upgrade

Wave 1 — Foundation:
- Consolidate design token sources (colors.ts, ThemeContext.tsx, designTokens.ts)
- Add *Subtle semantic tokens (successSubtle, dangerSubtle, warningSubtle)
- Add ThumbSize, AvatarSize, scrimTextPrimary/Secondary tokens
- Replace Duration with Motion.duration re-export
- Fix gradients to use isDark from ThemeContext
- Kill fabricated states: "coming soon" promises, fake "Saved" badge
- Fix FlagshipState: shimmer duration (M6), forbidden captionElevated (M12),
  bare FadeIn (M5), press scale (M3), shadow on primary (M17), hex-alpha (M13),
  dead noCard ternary (M14)
- Radius codemod: 137 raw borderRadius literals to Radius.* tokens (0 remaining)
- Hex-alpha cleanup: 37 backgroundColor patterns to *Subtle tokens
- Shadow cleanup: 4 raw shadows to Elevation.* tokens
- ActivityIndicator to FlagshipState: 7 full-screen spinners replaced
- Flagship primitive token fixes: FlagshipProductCard, FlagshipAssetCard,
  FlagshipOrderCard, FlagshipHeroSection, FlagshipProfileMedia

Wave 2 — Money surfaces:
- Discovery feed: category-tinted placeholder art direction, Pulse card
  flattening (card-on-card eliminated), window tabs repositioned below first
  media row, pulseBanner removed, Galleria editorial truthfulness (no-op press
  removed), AI-slop copy removed, LooksTab spinner to masonry skeleton,
  PremiumSkeletonTile reduced-motion gating
- PDP gallery+trust: category-sensitive focal points wired into
  CommerceMediaStage, shared transition support for focal-point images,
  gallery error state with retry, trust signals layer (verification badge,
  response time, buyer protection fallback)
- Navigation IA: tab bar labels added (recognition over recall), ClosetScreen
  migrated to FlagshipHeader, route grouping comments in types.ts +
  AppNavigator.tsx

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
